<?php include __DIR__ . "/../" . str_replace('circuite', 'chartere', basename(dirname(__FILE__))) . '/' . basename(__FILE__); ?>
<?php /*
import BaseFunctionality from '../common/travelfuse-travellers.js?newux=<?php echo !empty($_GET['newux']) ? (string)$_GET['newux'] : 1 ; ?>';

export default {
	name: <?php echo json_encode($a, JSON_UNESCAPED_SLASHES); ?>,
	extends: BaseFunctionality,
	props: {
		data: {
			type: Object,
			default: () => ({}),
		},
	},
	data(){
		return {
			key: '<?php echo ($k = basename(dirname(__FILE__)) . '/' . basename(__FILE__, '.js.php')); ?>',
			menu: {
				title: 'Calatori',
				placeholder: 'Unde pleci',
				search_label: 'Cauta destinatia',
				icon: 'mdi-flag-variant',
			},
			travellers: {
				ADT: (this.data['<?php echo $k; ?>'] || {}).ADT || 0,
				YTH: (this.data['<?php echo $k; ?>'] || {}).YTH || 0,
				CHD: (this.data['<?php echo $k; ?>'] || {}).CHD || [],
			},
		}
	},
	watch: {
	}
}
*/ ?>
